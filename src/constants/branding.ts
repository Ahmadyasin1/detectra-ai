/** Transparent wordmark (icon + text). */
export const DETECTRA_LOGO_SRC = '/detectra-logo.png';

/** Transparent icon-only mark for nav, favicon, and compact UI. */
export const DETECTRA_LOGO_MARK_SRC = '/detectra-logo-mark.png';

/** Retina mark for sharp display on HiDPI screens. */
export const DETECTRA_LOGO_MARK_2X_SRC = '/detectra-logo-mark@2x.png';

export const DETECTRA_BRAND_NAME = 'Detectra AI';

export const DETECTRA_TAGLINE = 'trusted multimodal intelligence';
